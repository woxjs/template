export default app => {
  // app.on('beforeCreate', () => {});
  // app.on('setup', options => {});
  // app.on('created', () => {});
  // app.on('beforeMount', () => {});
  // app.on('mounted', () => {});
}